# dataVisualizer
This is an UI based project which reads excel file and then lists out the columns once we arrange the column into a hierarchy, and select a chart, the project autoatically converts it into that interactive chart.
